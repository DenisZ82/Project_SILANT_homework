import { createTheme } from '@mui/material/styles';
// import "../stylse/Main.css";

const theme = createTheme({
    typography: {
      fontFamily: 'PT-Astra-Sans_Regular, Roboto, Helvetica, Arial, sans-serif',
    },
});

export default theme;